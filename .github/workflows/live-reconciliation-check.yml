name: Live Reconciliation Check (SUPERSEDED)

# Superseded 2026-07-25 by reconciliation-check-v3.yml -- see
# docs/EXPERIMENT-live-reconciliation.md for the full reason: this
# workflow's design (spawn `npm run dev` as a background process, poll
# for readiness) was inherently fragile in CI and never got past a
# consistent early failure. V3's build+serve-static+page.route() design
# is the proven, working replacement.
#
# No delete capability from chat's current toolset, so this is neutered
# instead of removed: push trigger dropped (it was firing on every
# commit to main for no reason), job replaced with a no-op. Real cleanup
# (actual file deletion) is a small follow-up for whoever next has
# direct repo access outside this constraint.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by reconciliation-check-v3.yml — see docs/EXPERIMENT-live-reconciliation.md"
