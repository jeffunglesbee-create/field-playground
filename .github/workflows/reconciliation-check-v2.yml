name: Reconciliation Check V2 (SUPERSEDED)

# Superseded 2026-07-25 by reconciliation-check-v3.yml. V2 existed to
# test one specific hypothesis (was a stale GitHub Actions workflow_dispatch
# cache the cause of repeated CI failures) -- it was, partially, but V2's
# own design (spawned dev server) still hit a different hang later in the
# same run. See docs/EXPERIMENT-live-reconciliation.md for the full chain.
#
# No delete capability from chat's current toolset -- neutered instead.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by reconciliation-check-v3.yml — see docs/EXPERIMENT-live-reconciliation.md"
